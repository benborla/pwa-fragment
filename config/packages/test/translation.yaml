framework:
    default_locale: 'en'
    translator:
        paths:
            - '%kernel.project_dir%/translations'
            - '%kernel.project_dir%/tests/Service/fixtures'
        fallbacks:
            - 'en'
